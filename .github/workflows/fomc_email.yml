name: Fed Monitor — Alert Email

# Event-triggered alerts (FOMC statement diff + press conference; Beige Book) are
# written to data/reports/fomc/ or data/reports/beige_book/ by their dedicated runs
# and emailed immediately on push — separate from the daily report so they go out the
# moment they land.

on:
  push:
    branches: [main]
    paths:
      - 'data/reports/fomc/**.txt'
      - 'data/reports/beige_book/**.txt'
  workflow_dispatch:

jobs:
  email:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history: the push may contain several commits

      - name: Email alert report(s) just pushed
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          BEFORE_SHA: ${{ github.event.before }}
          AFTER_SHA: ${{ github.sha }}
        run: |
          python3 - <<'EOF'
          import smtplib, os, subprocess, sys, re
          from email.mime.text import MIMEText

          # Alert report files changed anywhere in the PUSH RANGE (FOMC + Beige
          # Book). Diffing HEAD~1..HEAD missed alerts when a push carried more
          # than one commit (2026-09-17: three FOMC alerts were never emailed).
          before, after = os.environ.get("BEFORE_SHA", ""), os.environ.get("AFTER_SHA", "HEAD")
          rng = [before, after] if before and not re.fullmatch("0+", before) else ["HEAD~1", "HEAD"]
          out = subprocess.run(
              ["git", "diff", "--name-only", "--diff-filter=AM", *rng,
               "--", "data/reports/fomc/", "data/reports/beige_book/"],
              capture_output=True, text=True
          ).stdout
          files = [f for f in out.splitlines() if f.endswith(".txt")]
          if not files:
              print("No new FOMC report files in this push — skipping.")
              sys.exit(0)

          password = os.environ["GMAIL_APP_PASSWORD"].replace(" ", "")
          with smtplib.SMTP_SSL("smtp.gmail.com", 465) as server:
              server.login("revilresearch@gmail.com", password)
              for f in files:
                  body = open(f, encoding="utf-8").read()
                  # Subject = first non-empty line of the report (the run writes a clear title there).
                  title = next((ln.strip() for ln in body.splitlines() if ln.strip()), os.path.basename(f))
                  msg = MIMEText(body, "plain", "utf-8")
                  msg["Subject"] = f"Fed Monitor — {title}"
                  msg["From"] = "revilresearch@gmail.com"
                  msg["To"] = "thiago_teixeiraferreira@vanguard.com"
                  server.send_message(msg)
                  print(f"Sent: {f} -> {title}")
          EOF
