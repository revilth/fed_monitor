# Secrets
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs
logs/
*.log

# IDE
.vscode/
.idea/

# Historical corpus — large bulk download (BIS 366MB CSV, FRASER speeches,
# FOMC transcripts/minutes). Exceeds GitHub's 100MB file limit; must never be
# committed or the daily auto-push routine will fail. Kept locally + Drive only.
data/historical/

# Local Claude Code permissions — machine-specific, not shared
.claude/settings.local.json
